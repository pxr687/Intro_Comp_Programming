---
jupyter:
  jupytext:
    text_representation:
      extension: .Rmd
      format_name: rmarkdown
      format_version: '1.2'
      jupytext_version: 1.17.3
  kernelspec:
    display_name: Python 3 (ipykernel)
    language: python
    name: python3
---

# Exercise 3 - Data Types, Comparison Operators and Functions

If you need help, click here to open the _[textbook page](pxr687.github.io/Intro_Comp_Programming/1_07_Data_Types_Comparison_Operators_Functions.html)_ for this exercise.

## Instructions

**IMPORTANT**: If you see a comment which says `# DO NOT CHANGE THIS CODE` then **DO NOT CHANGE THAT CODE**. You will probably break the exercise if you do...

**First, run the code cell below**. Click on the cell, then press `Shift + Enter`:

```{python}
# RUN THIS CELL - DO NOT CHANGE THE CODE.
import numpy as np
import mark_exercise_3
# pop_thailand, are_they_the_same, answer_3, forty_two_as_float, thailand_and_myanmar_pop = mark_exercise_3.pre_define_blanks(5)
```

**ALSO IMPORTANT**: in **ALL** exercises, and **ALL** assignments/tests/exams. You **MUST** use the right variable names in your answers. 

If the question says "make a variable called `answer_1`" you **MUST** call you variable `answer_1` or you will **NOT** get a mark.

If the question says "make a variable called `answer_2`" you **MUST** call you variable `answer_2` or you will **NOT** get a mark.

...

If the question says "make a variable called `answer_50`" you **MUST** call you variable `answer_50` or you will **NOT** get a mark.

If the question says "make a variable called `me_and_my_cat`" you **MUST** call you variable `me_and_my_cat` or you will **NOT** get a mark.


### Downloading your work

*Binder and Google Colab will NOT save your work*. If you are working on Binder, when you finish all of the questions, you can download your work as an `.ipynb` file, by clicking the button shown below:

![](https://raw.githubusercontent.com/pxr687/Intro_Comp_Programming/refs/heads/main/images/download_your_work.png)

When you download your work, you can view it by uploading the file here: https://code-format.com/ipynb-viewer 

Later in the course we will install Python on your own laptop 🙂.


## Question 1

Imagine you are working on a project about the population of Thailand.

A Google search says that the [population of Thailand](https://www.worldometers.info/world-population/thailand-population/) is about 71.6 million people.

The cell below tries to assign a variable called `pop_thailand`, which has this number in.

However, the code is broken. See if you can fix it.

**NOTE: your answer MUST be a variable called `pop_thailand`, or the marking will not work.**

```{python tags=c("raise-exception")}
# Population of Thailand.
71600000 = pop_thailand 
```

### Run the next cell to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE.
# RUN THIS CELL TO MARK YOUR ANSWER.
mark_exercise_3.check_answer_1(pop_thailand)
```

## Question 2

Your friend is also working on the project about Thai population. They say that you should write 71.6 million in a different way, to make it easier for a human to read.

They say you can put an underscore (`_`) in the number. Your friend makes a variable called `better_pop_thailand` in the cell below.

```{python}
# Your friend's number.
better_pop_thailand = 71_600_000
```

Do `pop_thailand` and `better_pop_thailand` equal the same number? Is your friend right that you can put `_` in Python numbers? Or does how they have written the number change the meaning?

Make a variable called `are_they_the_same` which is the result of an expression which asks the question:

- *Do the variables `pop_thailand` and `better_pop_thailand` have the same value?*

**Rules:**

- you must use an operator to make your answer. 

- your answer must be a Boolean...BUT you must not hard-code it e.g. you cannot use `are_they_the_same = False`. 

See the _[textbook page](pxr687.github.io/Intro_Comp_Programming/1_07_Data_Types_Comparison_Operators_Functions.html)_ if you need to.

```{python}
# Type your answer here.
are_they_the_same = ...
```

### Run the next cell to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE.
# RUN THIS CELL TO MARK YOUR ANSWER.
mark_exercise_3.check_answer_2(are_they_the_same)
```

## Question 3

There is an idea that [meaning of life is 42](https://www.independent.co.uk/life-style/history/42-the-answer-to-life-the-universe-and-everything-2205734.html).

The cell below assigns a variable called `forty_two` which has the value `42`.

```{python}
# The meaning of life!
forty_two = 42
```

Make a variable called `answer_3` that has the `type` of `forty_two`.

**Rules**:

- you must use a function to do this, do not hard-code your answer!

```{python}
# Type your answer below.
answer_3 = ...
```

### Run the next cell to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE.
# RUN THIS CELL TO MARK YOUR ANSWER.
mark_exercise_3.check_answer_3(answer_3)
```

## Question 4

Make a variable called `forty_two_as_float` which changes the data type of `forty_two` to `float`.

```{python}
# Your answer here.
forty_two_as_float = ...
```

### Run the next cell to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE.
# RUN THIS CELL TO MARK YOUR ANSWER.
mark_exercise_3.check_answer_4(forty_two_as_float)
```

## Question 5

The [population of Myanmar](https://www.worldometers.info/world-population/myanmar-population/) is around 55.3 million.

The cell below stores this information in a variable called `pop_myanmar`:

```{python}
# The population of Myanmar.
pop_myanmar = '55_300_000'
```

We want to know: how many people are there in Thailand and Myanmar *together*?

Create a variable called `thailand_and_myanmar_pop` which contains the value of `pop_thailand + pop_myanmar`.

**Rules**:

- you must add the variables `pop_thailand` and `pop_myanmar` ... DO NOT just look at the numbers and add those. So, DO NOT use 
the numbers 71.6 million + 55.3 million.

```{python}
# Your answer here.
thailand_and_myanmar_pop = ...
```

### Run the next cell to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE.
# RUN THIS CELL TO MARK YOUR ANSWER.
mark_exercise_3.check_answer_5(thailand_and_myanmar_pop)
```

# Mark all your answers

```{python}
# DO NOT CHANGE THIS CELL. 
# Run this cell to mark all of your answers.
mark_exercise_3.mark_all(pop_thailand,
                         are_they_the_same,
                         answer_3,
                         forty_two_as_float,
                         thailand_and_myanmar_pop)
```

## Go back to the textbook:

Click _[here](pxr687.github.io/Intro_Comp_Programming/1_07_Data_Types_Comparison_Operators_Functions.html)_ to go back to the course textbook.
