---
jupyter:
  jupytext:
    text_representation:
      extension: .Rmd
      format_name: rmarkdown
      format_version: '1.2'
      jupytext_version: 1.17.3
  kernelspec:
    display_name: Python 3 (ipykernel)
    language: python
    name: python3
---

<!-- #region -->
# String methods

```{note}
This page is adapted from [Brett, Rush, Adhikari, Denero and Wagner (2023)](https://odsti.github.io/cfd-textbook/String_Methods.html). See the [license](https://github.com/pxr687/Intro_Comp_Programming/blob/main/license.md) page for more information.
```

From a string, related strings can be made using string methods. String methods are functions that operate on strings. These methods are called by placing a dot after the string, then calling the function.

For example, the following method generates an uppercased (all capitals) version of a string.
<!-- #endregion -->

```{python}
"loud".upper()
```

Perhaps the most important method is `replace`, which replaces all instances of a substring within the string. The `replace` method takes two arguments, the text to be replaced and its replacement.

```{python}
'hitchhiker'.replace('hi', 'ma')
```

String methods can also be used with variable names, as long as the variable is a string. So, for instance, the following two-step process makes the word "degrade" starting from "train" by first creating "ingrain"
and then applying a second replacement.

```{python}
s = "train"
t = s.replace('t', 'ing')
u = t.replace('in', 'de')
u
```

Let's break that down to make it clearer what is happening:

```{python}
# The string we start with.
s
```

```{python}
# Replace "t" with "ing".
t = s.replace('t', 'ing')
t
```

```{python}
# Replace "in" with "de"
u = t.replace('in', 'de')
u
```

Note that the line `t = s.replace('t', 'ing')` doesn't change the string `s`,
which is still "train".  The method call `s.replace('t', 'ing')` just has
a value, which is the string "ingrain".

```{python}
s
```

This is the first time we've seen methods, but methods are not unique to strings.  As we will see shortly, other types of objects can have them.

## Exercise - String Methods

If you are not already using this page with Binder or Live Code, launch it now:

![](https://raw.githubusercontent.com/pxr687/Intro_Comp_Programming/refs/heads/main/images/launch.JPG)

Below are some questions which you should answer by using string methods. All of the methods you need to solve these exercises are in the table below. Have a look at the table before doing the questions:

| Method                                                                           | Description                                                                                   |
| -------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| [`capitalize()`](https://www.w3schools.com/python/ref_string_capitalize.asp)     | Converts the first character to upper case                                                    |
| [`casefold()`](https://www.w3schools.com/python/ref_string_casefold.asp)         | Converts string into lower case                                                               |
| [`center()`](https://www.w3schools.com/python/ref_string_center.asp)             | Returns a centered string                                                                     |
| [`count()`](https://www.w3schools.com/python/ref_string_count.asp)               | Returns the number of times a specified value occurs in a string                              |
| [`encode()`](https://www.w3schools.com/python/ref_string_encode.asp)             | Returns an encoded version of the string                                                      |
| [`endswith()`](https://www.w3schools.com/python/ref_string_endswith.asp)         | Returns true if the string ends with the specified value                                      |
| [`expandtabs()`](https://www.w3schools.com/python/ref_string_expandtabs.asp)     | Sets the tab size of the string                                                               |
| [`find()`](https://www.w3schools.com/python/ref_string_find.asp)                 | Searches the string for a specified value and returns the position of where it was found      |
| [`format()`](https://www.w3schools.com/python/ref_string_format.asp)             | Formats specified values in a string                                                          |
| [`format_map()`](https://www.w3schools.com/python/ref_string_format_map.asp)     | Formats specified values from a dictionary in a string                                        |
| [`index()`](https://www.w3schools.com/python/ref_string_index.asp)               | Searches the string for a specified value and returns the position of where it was found      |
| [`join()`](https://www.w3schools.com/python/ref_string_join.asp)                 | Converts the elements of an iterable into a string                                            |
| [`ljust()`](https://www.w3schools.com/python/ref_string_ljust.asp)               | Returns a left justified version of the string                                                |
| [`lower()`](https://www.w3schools.com/python/ref_string_lower.asp)               | Converts a string into lower case                                                             |
| [`lstrip()`](https://www.w3schools.com/python/ref_string_lstrip.asp)             | Returns a left trim version of the string                                                     |
| [`maketrans()`](https://www.w3schools.com/python/ref_string_maketrans.asp)       | Returns a translation table to be used in translations                                        |
| [`partition()`](https://www.w3schools.com/python/ref_string_partition.asp)       | Returns a tuple where the string is parted into three parts                                   |
| [`replace()`](https://www.w3schools.com/python/ref_string_replace.asp)           | Returns a string where a specified value is replaced with a specified value                   |
| [`rfind()`](https://www.w3schools.com/python/ref_string_rfind.asp)               | Searches the string for a specified value and returns the last position of where it was found |
| [`rindex()`](https://www.w3schools.com/python/ref_string_rindex.asp)             | Searches the string for a specified value and returns the last position of where it was found |
| [`rjust()`](https://www.w3schools.com/python/ref_string_rjust.asp)               | Returns a right justified version of the string                                               |                                 |
| [`rsplit()`](https://www.w3schools.com/python/ref_string_rsplit.asp)             | Splits the string at the specified separator, and returns a list                              |
| [`rstrip()`](https://www.w3schools.com/python/ref_string_rstrip.asp)             | Returns a right trim version of the string                                                    |
| [`split()`](https://www.w3schools.com/python/ref_string_split.asp)               | Splits the string at the specified separator, and returns a list                              |
| [`splitlines()`](https://www.w3schools.com/python/ref_string_splitlines.asp)     | Splits the string at line breaks and returns a list                                           |                                   |
| [`strip()`](https://www.w3schools.com/python/ref_string_strip.asp)               | Returns a trimmed version of the string                                                       |
| [`swapcase()`](https://www.w3schools.com/python/ref_string_swapcase.asp)         | Swaps cases, lower case becomes upper case and vice versa                                     |
| [`title()`](https://www.w3schools.com/python/ref_string_title.asp)               | Converts the first character of each word to upper case                                       |
| [`translate()`](https://www.w3schools.com/python/ref_string_translate.asp)       | Returns a translated string                                                                   |
| [`upper()`](https://www.w3schools.com/python/ref_string_upper.asp)               | Converts a string into upper case                                                             |
| [`zfill()`](https://www.w3schools.com/python/ref_string_zfill.asp)               | Fills the string with a specified number of 0 values at the beginning                         |

(Table adapted from [W3Schools — Python String Methods](https://www.w3schools.com/python/python_ref_string.asp))

**Important: for every question in this exercise, DO NOT edit the string. E.g. do not change the letters in the string by typing new ones. Use string methods from the table above to edit each string, to get the correct answer.**

Fixing strings "manually" (e.g. by changing the string with your keyboard) works when we have one string...but in large datasets you could have 1000s or 1_000_000s of strings, then you will *have* to use string methods to edit them. So we will practice using string methods now, instead of directly editing the strings :)

### Question 1

Oops someone has made a spelling mistake with the name of this course. Fix this string using methods from the table above. When you fix the string, it should say "Introduction to Computer Programming". Store your corrected string in a variable called `answer_1`:

```{python}
answer_1 = "Intr0duction to Camapar Programming" # Use your string methods here!

# Show the value of `answer_1`.
answer_1
```

#### Run the cell below to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE!
from mark_string_methods_page import check_answer, homer
check_answer(answer_1, "Introduction to Computer Programming",
             question_name="Question 1",
             answer_name="answer_1")
```

### Question 2

Miranda is doing a presentation. The title of her presentation should be "Presentation on Climate Change". She stores this title as text in the variable `pres_title`. However, she has made some errors. 

```{python}
pres_title = "presentotion of climate change"

# Show the value.
pres_title
```

Use string methods to fix her errors so that `pres_title` returns the string `"Presentation on Climate Change"`.


#### Run the cell below to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE!
from mark_string_methods_page import check_answer
check_answer(pres_title, "Presentation on Climate Change",
             question_name="Question 2",
             answer_name="pres_title")
```

### Question 3

 Mark is messaging his friend. His friend has done something funny, and Mark what to reply with this:

```{python}
text_reply  = "OMG I CANNOT BELIEVE YOU DID THAT!!!!!!"

# Show the value.
text_reply 
```


However, he didn't mean to use all capital letters. He thinks the message sounds too angry, and he wants to change it to `'omg i cannot believe you did that!!!!!!'` so that it sounds more like he is laughing with his friend, instead of sounding angry.

Fix Mark's reply using string methods, and save you answer in the `text_reply` variable.


#### Run the cell below to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE!
import numpy as np
from mark_string_methods_page import check_answer
check_answer(text_reply, "omg i cannot believe you did that!!!!!!",
             question_name="Question 3",
             answer_name="text_reply")
```

### Question 4

Chan is writing an essay for a History class. The essay title should be "Essay on the Roman Republic".

However, Chan was working late, and fell asleep on her keyboard. She saved the essay title in the variable `essay_title`, but because her head was resting on the keyboard, she has lots loads of spaces by accident.

Her string at the moment is 

```
"                                                           Essay on the Roman Republic"
```

Use string methods to fix her title, so that it does not have the extra spaces

```
"Essay on the Roman Republic"
```

Save your answer in the `essay_title` variable. Remember **DO NOT** edit her string by deleting the space with your keyboard. You **must** use a string method.

```{python}
essay_title  = "                                                                     Essay on the Roman Republic"

# Show the value.
essay_title
```


#### Run the cell below to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE!
from mark_string_methods_page import check_answer
check_answer(essay_title, "Essay on the Roman Republic",
             question_name="Question 4",
             answer_name="essay_title")
```

### Question 5

 In the cell below, the variable `book_summary` contains the summary of a book called ["Missing Link"](https://www.gutenberg.org/ebooks/23210), by Frank Herbert:

```{python}
# DO NOT CHANGE THIS CODE.
book_summary = '"Missing Link" by Frank Herbert is a science fiction short story originally published in the February 1959 issue of "Astounding Science Fiction". Set in a fictional universe where interstellar exploration and alien encounters are common, the narrative explores the complexities of first contact with an alien civilization that possesses advanced technology derived from a lost human ship. The story delves into themes of communication, cultural exchange, and the unforeseen consequences of human actions in unfamiliar territories. The plot centers on Lewis Orne, a junior field agent tasked with establishing contact with the inhabitants of Gienah III after the disappearance of the ship "Delphinus Rediscovery". As Orne navigates the jungle-laden planet, he encounters Tanub, a Gienahn who holds an advanced alien rifle, suggesting that they have recovered the wreckage of "Delphinus". As tension rises, Orne must negotiate with Tanub and his people to prevent an inevitable extermination by their Terran overlords. The story develops through a series of discussions exploring the nature of power, technology, and the potential for coexistence between different cultures, ultimately leading to a precarious diplomatic resolution.'

# Show the value.
book_summary
```


Use string methods to count the number of times the word `"alien"` appears in the summary. Save your answer in a variable called `count_alien`.

As always **DO NOT** hard-code your answer (e.g. by counting yourself and typing the number). Use string methods.

```{python}
# Your answer here.
count_alien = ...

# Show the value.
count_alien
```

#### Run the cell below to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE!
from mark_string_methods_page import check_answer
check_answer(count_alien, 3, 
             correct_is_string=False,
             question_name="Question 5",
             answer_name="count_alien")
```

### Question 6

Look again at the `book_summary` from the last question...

```{python}
book_summary
```

Use string methods to count how many times the word `"alien"` AND the word `"technology"` are in `book_summary`.

Your answer should be the number of times *both* words are in the `book_summary`. So if it says `"alien"` 6 times and `"technology"` 6 times, your answer should be 12 etc.

Save your answer in a variable called `count_alien_and_technology`:

```{python}
count_alien_and_technology = ...

# Show the value.
count_alien_and_technology
```


*Note*: counting words like this is used in [sentiment analysis](https://en.wikipedia.org/wiki/Sentiment_analysis), which uses machine learning to classify emotion (and other things) in text data.


#### Run the cell below to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE!
from mark_string_methods_page import check_answer
check_answer(count_alien_and_technology, 5,
            correct_is_string=False,
             question_name="Question 6",
             answer_name="count_alien_and_technology")
```

### Question 7

Here is another [book summary](https://www.gutenberg.org/ebooks/6130), stored in a variable called `another_book_summary`:

```{python}
another_book_summary = "'tHE iLIAD' BY hOMER IS AN ANCIENT gREEK EPIC POEM COMPOSED AROUND THE LATE 8TH OR EARLY 7TH CENTURY bc. sET DURING THE FINAL WEEKS OF THE TEN-YEAR tROJAN wAR, IT FOLLOWS THE DEVASTATING ANGER OF aCHILLES, THE GREATEST gREEK WARRIOR, SPARKED BY A BITTER QUARREL WITH kING aGAMEMNON. aS PRIDE AND WRATH COLLIDE, THE CONFLICT ESCALATES FROM PERSONAL DISPUTE TO BATTLEFIELD TRAGEDY, CULMINATING IN THE DEATH OF tROY'S CHAMPION, hECTOR. gODS INTERVENE, HEROES CLASH, AND THE FATE OF NATIONS HANGS IN THE BALANCE."
another_book_summary
```

Ooh, something has gone wrong with that. Can you work out how to fix it with a string method?

Here is how `another_book_summary` should look, when it is correct:

```
"'The Iliad' by Homer is an ancient Greek epic poem composed around the late 8th or early 7th century BC. Set during the final weeks of the ten-year Trojan War, it follows the devastating anger of Achilles, the greatest Greek warrior, sparked by a bitter quarrel with King Agamemnon. As pride and wrath collide, the conflict escalates from personal dispute to battlefield tragedy, culminating in the death of Troy's champion, Hector. Gods intervene, heroes clash, and the fate of nations hangs in the balance."
```

Save your answer in a variable called `correct_summary`:

```{python}
correct_summary  = another_book_summary # Add you methods here!

# Show the value.
correct_summary
```


#### Run the cell below to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE!
from mark_string_methods_page import check_answer
check_answer(correct_summary, homer(),
             question_name="Question 7",
             answer_name="correct_summary")
```

### Question 8

Davic is working on a project called "The Business Case For A.I.".

He saved his work in a Word document. But he has been downloading lots of movies from pirate websites.
 As a result, his computer got a virus, and his filename has been corrupted!

His variable `project_name` should be this string `"The Business Case For A.I."`. However, at the moment it is:

```
"                       the bosiness cese for A.I.                                               "
```

Fix it using string methods and store your answer in the variable `project_name`:

```{python}
project_name  = "                       the bosiness cese for A.I.                                               "

# Show the value.
project_name
```


#### Run the cell below to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE!
from mark_string_methods_page import check_answer
check_answer(project_name, "The Business Case For A.I.",
             question_name="Question 8",
             answer_name="project_name")
```

### Question 9

Here is some text from a [Wikipedia]() page:

```
"**The British are coming** is a phrase misattributed to [Paul Revere's Midnight Ride](https://en.wikipedia.org/wiki/Paul_Revere's_Midnight_Ride 'Paul Revere's Midnight Ride')."
```

It is *Markdown text*, it is written so that it looks nice in a Markdown cell (it has some **bold** text and a hyperlink):

>"**The British are coming** is a phrase misattributed to [Paul Revere's Midnight Ride](https://en.wikipedia.org/wiki/Paul_Revere's_Midnight_Ride "Paul Revere's Midnight Ride")."

However, the string has been saved in a file which is the wrong format, and some of the text has changed.

The cell below contains that broken string, in a variable called `wiki_text`. The broken text is:

```
"##The British are coming## is a phrase misattributed to <Paul Revere's Midnight Ride>(http//en.wikipedia.org/wiki/Paul_Revere's_Midnight_Ride 'Paul Revere's Midnight Ride')."
```

Fix it so that it is:
```
"**The British are coming** is a phrase misattributed to [Paul Revere's Midnight Ride](https://en.wikipedia.org/wiki/Paul_Revere's_Midnight_Ride 'Paul Revere's Midnight Ride')."
```

Save your answer in a the `wiki_text` variable.

```{python}
wiki_text  = "##The British are coming## is a phrase misattributed to <Paul Revere's Midnight Ride>(http//en.wikipedia.org/wiki/Paul_Revere's_Midnight_Ride 'Paul Revere's Midnight Ride')."

# Show the value.
wiki_text
```


#### Run the cell below to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE!
from mark_string_methods_page import check_answer
check_answer(wiki_text, 
            "**The British are coming** is a phrase misattributed to [Paul Revere's Midnight Ride](https://en.wikipedia.org/wiki/Paul_Revere's_Midnight_Ride 'Paul Revere's Midnight Ride').",
            question_name="Question 9",
             answer_name="wiki_text")
```

----------------------------------------------------------

[🏡](intro.md).
