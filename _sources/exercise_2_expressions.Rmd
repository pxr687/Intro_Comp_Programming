---
jupyter:
  jupytext:
    text_representation:
      extension: .Rmd
      format_name: rmarkdown
      format_version: '1.2'
      jupytext_version: 1.17.3
  kernelspec:
    display_name: Python 3 (ipykernel)
    language: python
    name: python3
---

# Exercise 2 - Expressions

## Instructions

**THIS SUPER IMPORTANT**: If you see a comment which says `# DO NOT CHANGE THIS CODE` then **DO NOT CHANGE THAT CODE**. You will probably break the exercise if you do...

**First, run the code cell below**. Click on the cell, then press `Shift + Enter`:

```{python}
# RUN THIS CELL - DO NOT CHANGE THE CODE.
import numpy as np
import mark_exercise_2
answer_1, answer_2, answer_3, answer_4, answer_5, = mark_exercise_2.pre_define_blanks(5)
```

**THIS ALSO SUPER IMPORTANT**: in **ALL** exercises, and **ALL** assignments/tests/exams. You **MUST** use the right variable names in your answers. 

If the question says "make a variable called `answer_1`" you **MUST** call you variable `answer_1` or you will **NOT** get a mark.

If the question says "make a variable called `answer_2`" you **MUST** call you variable `answer_2` or you will **NOT** get a mark.

...

If the question says "make a variable called `answer_50`" you **MUST** call you variable `answer_50` or you will **NOT** get a mark.


If you are working on Binder, when you finish all of the questions, you can download your work as an `.ipynb` file.

![](https://raw.githubusercontent.com/pxr687/Intro_Comp_Programming/refs/heads/main/images/download_your_work.png)

Your can view you work by uploading the file here: https://code-format.com/ipynb-viewer 

Later in the course we will install Python on your own laptop 🙂.


## Question 1

You work in a Physics lab, and your co-worker has made lots of mistakes in some code.

In the cell below, there is an assignment statement: `answer_1 = 100 * 2`.

`answer_1` returns the value `200`. 

Your boss tells you that `answer_1` should equal `10000`, not `200`! They ask you to fix you co-worker's mistake.

Change the expression `100 * 2` so that `answer_1` evaluates to `10000`.

Rules:

- You can only add, remove, or change operator  (`+`, `-`, `/`, `*`, `**`) or brackets (`()`).
- You *cannot*  change the numbers 100 or 2.
- The final value of `answer_1` must be `10000`.

**DO NOT USE `answer_1 = 10000`. This is called hard-coding an answer...it is not allowed in any exercise or assessment.**

```{python}
# Type your answer below.
answer_1 = 100 * 2

# Show the value of `answer_1`. 
answer_1
```

### Run the next cell to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE.
# RUN THIS CELL TO MARK YOUR ANSWER.
mark_exercise_2.check_answer_1(answer_1)
```

## Question 2

You co-worker has made ANOTHER mistake!

In the code cell below, there is an assignment statement:

`answer_2 = 8 + 4 / 2`

The value of `answer_2`, at the moment, is `10`, but it should be `16`.

Change the expression `8 + 4 / 2` so that `answer_2` evaluates to `16`.

Rules:

- You can only add, remove, or change operator  (`+`, `-`, `/`, `*`, `**`) or brackets (`()`).
- You *cannot*  change any numbers.
- The final value of `answer_2` must be `16`.

**DO NOT USE `answer_2 = 16`. This is called hard-coding an answer...it is not allowed in any exercise or assessment.**

```{python}
# Type your answer below.
answer_2 = 8 + 4 / 2

# Show the value of `answer_2`. 
answer_2
```

### Run the next cell to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE.
# RUN THIS CELL TO MARK YOUR ANSWER.
mark_exercise_2.check_answer_2(answer_2)
```

## Question 3

Your co-worker has been fired. But you still need to fix more of their mistakes!!!

In the cell below there is a variable called `answer_3`. 

On the right of the assignment operator (`=`) is the expression `2 ** 2 * 4 + 6`.

At the moment, the variable `answer_3` has the value `22`, but it should evaluate to `40`.

Change the expression `2 ** 2 * 4 + 6` so that `answer_2` evaluates to `40`.

Rules:

- You can only add, remove, or change operator  (`+`, `-`, `/`, `*`, `**`) or brackets (`()`).
- You *cannot*  change any numbers.
- The final value of `answer_3` must be `40`.

**DO NOT USE `answer_3 = 40`. This is called hard-coding an answer...it is not allowed in any exercise or assessment.**

```{python}
# Type your answer below.
answer_3 = 2 ** 2 * 4 + 6

# Show the value of `answer_3`. 
answer_3
```

### Run the next cell to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE.
# RUN THIS CELL TO MARK YOUR ANSWER.
mark_exercise_2.check_answer_3(answer_3)
```

## Question 4

`answer_4` is a variable which has the result of `8 - 6 * 2 + 24`. 

At the moment the expression evaluates to `20`. 

It should give the value `28`.

Add/change operators (`+`, `-`, `/`, `*`, `**`) and/or brackets (`()`) in the expression, to make it equal `28`.

**DO NOT USE `answer_4 = 28`. This is called hard-coding an answer...it is not allowed in any exercise or assessment.**

```{python}
# Type your answer below.
answer_4 = 8 - 6 * 2 + 24

# Show the value of `answer_4`
answer_4
```

### Run the next cell to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE.
# RUN THIS CELL TO MARK YOUR ANSWER.
mark_exercise_2.check_answer_4(answer_4)
```

## Question 5

`answer_5 = 9 * 4 + 8 / 2 - 3` returns the value `37`. 

Change the operators or brackets so that it has the value `28`.

```{python}
# Type your answer below.
answer_5 = 9 * 4 + 8 / 2 - 3

# Show the value of `answer_5`.
answer_5
```

### Run the next cell to mark your answer.

```{python}
# DO NOT CHANGE THIS CODE.
# RUN THIS CELL TO MARK YOUR ANSWER.
mark_exercise_2.check_answer_5(answer_5)
```

# Mark all your answers

```{python}
# DO NOT CHANGE THIS CELL. 
# Run this cell to mark all of your answers.
mark_exercise_2.mark_all(answer_1,
                         answer_2,
                         answer_3,
                         answer_4,
                         answer_5)
```

## Go back to the textbook:

Click [here](https://pxr687.github.io/Intro_Comp_Programming/intro.html) to go back to the course textbook.
