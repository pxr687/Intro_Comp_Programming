---
jupyter:
  jupytext:
    text_representation:
      extension: .Rmd
      format_name: rmarkdown
      format_version: '1.2'
      jupytext_version: 1.17.3
  kernelspec:
    display_name: Python 3 (ipykernel)
    language: python
    name: python3
---

<!-- #region -->
# Introduction to Python and Jupyter Notebook 

*Note*: This pages cites and draws inspiration from [Brett et al, 2023](https://odsti.github.io/cfd-textbook/the_software.html).

## Python

For this course, we will be using Python.

The advantages of Python are:

> - Python code is easy to read and write.
> - It has a very wide range of libraries, including many libraries for
>   tasks in science, such as data analysis, statistics and visualization.
>   For example, there are libraries for designing and running Psychology
>   experiments.
> - It is widely used in teaching, science and industry.
> - Because it has a large open-source community, it is easy to find other
  people working on the same thing as you, in Python. The community has
  a strong background in good practices for code, such as testing, and
  writing code that is clear and simple.
>
> *Brett et al, 2023* 

![](https://raw.githubusercontent.com/pxr687/Intro_Comp_Programming/refs/heads/main/images/python_growth.JPG)
(Image from [here](https://stackoverflow.blog/2017/09/06/incredible-growth-python))


Have a look at [this page](https://docs.python.org/3/faq/general.html) for some extra information about Python - including why it is called "Python"!
<!-- #endregion -->

## Jupyter Notebooks

This textbook is written using Jupyter Notebooks. Inside a Jupyter Notebook (an `.ipynb` file) we can write and run Python code. We will use Jupyter Notebooks for the start of this course. Later, we will install Python on your personal laptop.

When using this textbook you should run the code yourself - do not just read the text. 

Click this button at the top of the page to launch this page Binder, where you can run the Python code as well as read the text:

![](https://raw.githubusercontent.com/pxr687/Intro_Comp_Programming/refs/heads/main/images/launch.JPG)

Or click [here 🚀](https://mybinder.org/v2/gh/pxr687/Intro_Comp_Programming/main?urlpath=tree/1_3_Introduction_to_Python_and_Jupyter.ipynb). 

**Note:** it can take a few minutes to launch the notebook online...


## Markdown cells

Jupyter Notebooks are made from *cells*.

Your are reading the text in a *Markdown* cell.

We can put text, images, tables and [web hyperlinks](https://jupyter-notebook.readthedocs.io/en/stable/examples/Notebook/Notebook%20Basics.html) inside a Markdown cell.

A Markdown cell does NOT run code.

Everything you have read in this book so far is inside of a Markdown cell.


## Code cells

The cell below is a code cell.

As you would expect, this cell runs code. We will now see our first bit of Python code!

*Note*: you can press `Shift + Enter` on your keyboard to run a code cell:

```{python}
2 + 2
```

What happened here? 

When I (or you!) pressed `Shift + Enter` Jupyter Notebook sent the input (`2 + 2`) to the Python compiler (also called an interpreter).

The compiler added 2 and 2 together, and showed the output.

Here is some more, simple Python code:

```{python}
print("Hello world!")
```

What happened here? 

When I (or you!) pressed `Shift + Enter` my Jupyter Notebook sent the input - this time the input is text, not numbers - to the Python interpreter.

The text is inside the `print()` function. All this does is takes input and...prints it out, as you probably guessed!

We will talk more lateer about text and numbers in Python, for now, try changing the text in the cell below to make it say something you want it to say:

*Note*: one Python rule is that you have to put text in quotes, `"` or `'`. You will get an error message if you do not.

```{python}
print("Change this text!")
```

We have seen how to run code in code cells.

We can also write *comments* inside code cells. A comment is **NOT** sent to the Python compiler. This means it is **NOT** run as Python code. To make a comment, we put the `#` symbol at the start of the line:

```{python}
# This is a comment. It is not run as Python code, even when we press `Shift + Enter`
```

So, when you run a cell which only has comments inside...nothing happens!

Comments are useful for *humans*. They explain what the code does:

```{python}
# This is a comment. It is not run as Python code, even when we press `Shift + Enter`
# This comment explains what the code does. It prints out "Hello class!"
print("Hello class!")
```

### References

Brett, M., Rush, P., Adhikari, A., Denero, J., & Wagner, D. (2023). *Our tools.* In *Coding for data.* https://odsti.github.io/cfd-textbook/the_software.html
